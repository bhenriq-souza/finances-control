# ADR-0001 — Stack do front-end

- Status: **open** (decisão pendente)

## Context

A plataforma será acessível via web com interface amigável (requisito de negócio §1). Nenhuma decisão de stack foi tomada ainda. O ecossistema do usuário é TypeScript.

## Candidatos a avaliar

- React + Vite (histórico do usuário com React)
- Next.js (SSR desnecessário para app interna? avaliar)
- Considerar: biblioteca de componentes, autenticação integrada com o backend (ADR-0006), empacotamento como imagem OCI para o pipeline existente.

## Decision

_A definir — tarefa FC-001 no [backlog](../backlog.md)._
